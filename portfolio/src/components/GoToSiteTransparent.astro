---
import GoToSite from '@components/GoToSite.astro';

const background_color = "transparent";;
interface Props {
	text: string;
	svg: string;
    url: string;
	target: string;
}

const { 
    text,
    svg,
    url,
	target,
} = Astro.props;
---


<GoToSite
    text={text}
    svg={svg}
    url={url}
	target={target}
    background_color={background_color}
></GoToSite>