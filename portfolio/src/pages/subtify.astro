---
import Layout from '@layouts/Layout.astro';

const { social_links } = await import('@portfolio/consts.json');
const { metadata_page } = await import('@portfolio/consts.json');
const { colors } = await import('@portfolio/consts.json');
const { color_palette } = await import('@portfolio/consts.json');

const page_title = metadata_page.title + " - Subtify";
const url = social_links.portfolio_link_external + "/subtify"
const description = "Generador de subtítulos para videos en el idioma que desees. Además a cada persona le pone su subtítulo de un color";
const keywords = "Generador de subtítulos, subtítulos, videos, idiomas, colores";

const subtify_url = "https://maximofn-subtify.hf.space";
const huggingFace_subtify_url = "https://huggingface.co/spaces/Maximofn/subtify";

const inline_code_background_color = color_palette.color_950;
const color_white = colors.color_white;
---

<Layout 
	title={page_title}
	languaje={metadata_page.languaje_es}
	description={description}
	keywords={keywords}
	author={metadata_page.author}
	theme_color={colors.background_color}
	url={url}
	icon={metadata_page.icon}
>
	<div class="project">
		<h1>Subtify</h1>
		<h4>{description}</h4>
		<p>A continuación puedes probar la aplicación de gradio embebida en esta página. Si prefieres puedes acceder a la <a href={subtify_url} target="_blank" rel="nofollow noreferrer">aplicación</a> directamente, o ir al <a href={huggingFace_subtify_url} target="_blank" rel="nofollow noreferrer">espacio de huggingface</a>.</p>

		<!-- <script
			type="module"
			src="https://gradio.s3-us-west-2.amazonaws.com/3.49.0/gradio.js"
		></script>
		<gradio-app src="https://maximofn-subtify.hf.space"></gradio-app> -->

		<iframe
			src="https://maximofn-subtify.hf.space"
			frameborder="0"
		></iframe>
	</div>

</Layout>

<style define:vars={{
	inline_code_background_color,
	color_white,
}}>
	.project {
		display: flex;
		flex-direction: column;
	}

	h1 {
		margin-bottom: 0px;
	}

	h4 {
		padding-left: 0px;
	}

	a {
		color: var(--color_white);
	}

	iframe {
		width: 100%;
		height: 100vh;
		border: 1px solid var(--inline_code_background_color);
		box-shadow: 3px 3px 6px 0 hsla(0, 0%, 100%, 0.3);
		border-radius: 7px;
	}
</style>
